#Donatello
##Or, Application for Remote Downloading of Large Media Files.
Provides a web service, written in PHP, which web stores can plug into, and a
Windows application, written in C# .NET, which allows purchased items via the
web service to immediately commence downloading on a remote machine running
the application as a Windows service.
